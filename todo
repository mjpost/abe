1. the beams weren't sorted before return
    - we fixed this, but is happening elsewhere
2. double/triple check that tokenization is consistent with models concerning the language codes (default is to append)
3